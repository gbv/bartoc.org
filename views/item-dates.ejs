<%
  const { created, issued, modified } = item
  if (created || issued || modified) { %>
  <ul class="item-dates separated-list text-right">
   <% if (created) { %>
    <li><small>created <time><%= created %></time></small></li>
   <% } %>
   <% if (issued) { %>
    <li><small>issued <time><%= issued %></time></small></li>
   <% } %>
   <% if (modified && modified !== created && modified !== issued) { %>
    <li><small>modified <time><%= modified %></time></small></li>
   <% } %>
  </ul>
<% } %>
