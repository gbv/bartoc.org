<%
  for (let lang in (abstract || {})) {
    for (let text of abstract[lang]) {
%>
 <p lang="<%= lang %>" class="language-tag">
  <%- String(text).split(/\n+/).map(utils.escapeXML).join("</p><p>") %>
 </p>
<%
   }
  }
%>
